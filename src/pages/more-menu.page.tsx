import React from 'react';
import { View } from 'react-native';

/** Tab giữ — không hiển thị nội dung, chỉ mở dropdown từ bottom bar */
const MoreMenuPage = () => <View className="flex-1 bg-white" />;

export default MoreMenuPage;
